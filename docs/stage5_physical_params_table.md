# 实车物理参数实测表

> 对应 [`stage5.md`](stage5.md) 步骤 ①；高度一律相对**轮轴中心**（车轮圆心），轴上方为正。  
> 若从地面量：\(h = h_{\mathrm{离地}} - r\)，\(r=\) 轮半径。

---

## 0. \(M\) / \(m\) 怎么拆（先读再称）

经典两轮倒立摆里：

| 符号 | 包含什么 | 实车怎么称 |
| --- | --- | --- |
| **\(M\)（车体）** | 会「跟着车身俯仰」的质量：骨架、电池、开发板、驱动板、**电机定子/壳体（整颗电机可不拆）**、线材等 | **拆掉左右车轮**后上秤；**电机留在车上** |
| **\(m\)（轮侧）** | 两只轮子 +（可选）转子转动惯量的等效质量 | 单独称两轮；转子惯量没有现成设备时先只用两轮质量，或沿用「电机+轮」整称再拆 |

**结论：\(M\) 要去掉车轮，但不要去掉电机。**  
电机壳体固定在车架上，属于车体；轮子才是相对车体转动的那部分。

本车历史粗分（[`balance_gain_theory.md`](balance_gain_theory.md#this-robot)）：电机+轮 ≈ 0.515 kg，上层 ≈ 0.260 kg，整车 ≈ 0.775 kg——下面表格用来重测、细分。

由分段质量估车体质心高度（相对轮轴）：

\[
l_M \approx \frac{\sum m_i h_i}{\sum m_i}
\]

**符号：轮轴中心为 0，轴上方 \(h_i>0\)，轴下方 \(h_i<0\)。**  
电机壳体若挂在轴下，填负的 \(h_i\)，加权里就是 \(+m_{\mathrm{电机}}(-|h|)\)，等价于从分子里减掉，不必另写一步「减去」。\(l_M\) 仍是带符号的结果：正 = 质心在轴上（倒立摆），接近 0 或为负则模型假设不成立，先复查量法。

若模型的 \(l\) 要用**整车含轮**质心，再把轮（\(h_{\mathrm{wheel}}\approx 0\)）并进去加权即可；多数 LQR 脚本里 \(l\) 是**车体质心到轮轴**，用 \(l_M\) 即可。轮质量**不要**并进 \(l_M\)。

---

## 1. 几何（先填）

| 量 | 符号 | 测法 | 填入 | 单位 |
| --- | --- | --- | --- | --- |
| 轮半径 | \(r\) | 外径/2 或滚距反推 | ______ | m |
| 轮距 | \(d\) / `kWheelDistanceM` | 两轮接地中心距 | ______ | m |
| 轮轴离地 | \(=r\) | 直立时轴心到地面 | （应≈\(r\)） | m |

日期：________ 操作：________

---

## 2. 分段质量与高度（相对轮轴）——填这张主表

称重时车轮已拆下（测 \(M\) 分段）；高度用直尺/卡尺量零件**几何中心或重心近似**到轮轴的竖直距离。  
**不要把「电机 + 骨架」合成一行**：电机 \(h\) 在轴附近（可负），骨架在轴上，混在一起会把 \(l_M\) 系统性压低。

| 部件 | 质量 \(m_i\) (g) | 相对轮轴高度 \(h_i\) (m) | \(m_i h_i\) (g·m) | 备注 |
| --- | ---: | ---: | ---: | --- |
| 电池+电流传感器 | 160 | 0.035 | 5.60 | 含电池盒则整盒一起称 |
| 上层骨架 / 车架（含板、不含电池） | 120 | 0.07 | 8.40 | 若骨架拆不开，可与板合并一行 |
| 电机上层金属骨架 | 130 | 0.02 | 2.60 | |
| 左电机（含减速箱，**不含轮**） | 145 | -0.008 | -1.16 | 轴下为负 |
| 右电机（含减速箱，**不含轮**） | 145 | -0.008 | -1.16 | 轴下为负 |
| 其它（线材、螺丝、传感器支架…） | | | | 可合并「杂项」 |
| **合计 = \(M\)** | 700 | — | **14.28** | 应等于「去轮整车」一次称重 |

验算：

| 项 | 数值 |
| --- | ---: |
| 去轮一次上秤（应 ≈ \(M\)） | 0.70 kg（分段合计） |
| \(l_M = (\sum m_i h_i)/M\) | \(14.28/700=0.0204\) m |
| 旧估算 `kComHeightM` | 0.012 m |
| 偏差 | +0.008 m |

**电机行**：拆轮后电机仍装在车上称最省事；若只能单称电机，左右分开填。质心相对轴心量竖直距离，**在轴下填负数**，不要先取绝对值再另减一次。

---

## 3. 轮侧质量 \(m\)

| 部件 | 质量 (g) | 相对轮轴高度 | 备注 |
| --- | ---: | ---: | --- |
| 左轮 | 45 | ≈ 0 | 轮胎+轮毂，不含电机 |
| 右轮 | 45 | ≈ 0 | |
| **两轮合计（记入 \(m\) 的主体）** | 90 | 0 | |
| （可选）电机+轮整称 − 去轮后电机估重 | ______ | — | 与上交叉核对 |

仿真脚本若把 \(m\) 定义为「单轮」还是「两轮之和」，填表后对照 `lqr_gain_design.py` 输入说明，**不要混单/双**。

### 3.1 「电机转子等效」是什么（不是把电机再称一次）

\(m\) 里写的「两轮 + 电机转子等效」指的是：

- **两轮**：真实平动/转动质量（上表称出来即可）  
- **电机转子等效**：转子有转动惯量 \(J_{\mathrm{m}}\)，经减速比反映到轮轴后，在「轮子平动」模型里等价成一截质量 \(m_{\mathrm{rot}}\)  

**不是**把整颗电机质量加进 \(m\)（那样会和 \(M\) 重复）。  
电机壳体/减速箱外壳仍在 \(M\)；只有**跟着电枢转的那部分惯量**才折算进 \(m\)。

单侧折算（输出轴 = 轮轴，减速比 \(n=\omega_{\mathrm{电机}}/\omega_{\mathrm{轮}}\)）：

\[
J_{\mathrm{out}} = J_{\mathrm{m}}\, n^{2}
\quad\Rightarrow\quad
m_{\mathrm{rot,1}} = \frac{J_{\mathrm{out}}}{r^{2}} = J_{\mathrm{m}}\frac{n^{2}}{r^{2}}
\]

两轮车：\(m_{\mathrm{rot}}=2\,m_{\mathrm{rot,1}}\)（左右各一颗）。  
本车 P30：\(n=30\)，\(r\approx0.0375\,\mathrm{m}\)。

### 3.2 怎么估算 \(J_{\mathrm{m}}\) / \(m_{\mathrm{rot}}\)（由易到难）

| 档 | 做法 | 适用 |
| --- | --- | --- |
| **A. 先忽略转子** | \(m=\) 两轮质量之和；电机全进 \(M\) | **推荐起步**；\(m\) 略偏小，用 Q/R 留裕度 |
| **B. 铭牌/手册** | 查 MG513 电枢惯量 \(J_{\mathrm{m}}\)（kg·m² 或 g·cm²），代入上式 | 有数据时最干净 |
| **C. 粗经验** | 转子质量取电机质量的 ~10%～20%，等效在电枢半径 \(r_{\mathrm{a}}\) 上：\(J_{\mathrm{m}}\approx m_{\mathrm{rotor}} r_{\mathrm{a}}^{2}/2\)，再 \(\times n^{2}/r^{2}\) | 无手册时数量级估计；\(r_{\mathrm{a}}\) 约几 mm，算完看 \(m_{\mathrm{rot}}\) 是否离谱 |
| **D. 台架辨识** | 已知 \(\tau\) 或电流，空载加速 \(\dot\omega\)：\(J\approx\tau/\dot\omega\)（含齿轮），再扣轮的 \(J_{\mathrm{wheel}}=m_{\mathrm{wheel}}r^{2}\) | 有电流/力矩后做 |

**数量级直觉（P30，\(n=30\)）**：\(n^{2}=900\)，很小的 \(J_{\mathrm{m}}\) 也会被放大；但玩具减速电机 \(J_{\mathrm{m}}\) 本身极小，算完 \(m_{\mathrm{rot}}\) 常常只有**几十克到与单轮同量级**，很少超过两轮质量很多。若粗算得到 \(m_{\mathrm{rot}}\) 比整车还重，多半是 \(J_{\mathrm{m}}\) 或单位错了。

### 3.3 实车建议怎么填

1. **第一次算 LQR**：\(m=\) 左右轮质量之和，\(m_{\mathrm{rot}}=0\)（档 A）。  
2. 电流/挂重通了以后，有空再做档 D，把 \(m_{\mathrm{rot}}\) 补进 \(m\) 重算 \(K\)。  
3. 不要把「电机+轮 0.515 kg」整段塞进 \(m\)，同时 \(M\) 又含电机——会双计。

---

## 4. 汇总给 LQR / 笔记

| 符号 | 含义 | 填入 | 单位 |
| --- | --- | --- | --- |
| \(M\) | 车体（去轮、留电机） | 0.70 | kg |
| \(m\) | 轮侧（两轮合计，转子=0） | 0.090 | kg |
| \(l\) | 车体质心→轮轴（§2 的 \(l_M\)） | 0.0204 | m |
| \(r\) | 轮半径 | 0.0375 | m |
| \(I\) | **车体绕质心**俯仰惯量 \(I_{\mathrm{cm}}\) | 第一次用 **0**；分段估 \(5.63\times10^{-4}\) | kg·m² |
| 整车总重 | \(M+m_{\mathrm{两轮}}\) | 0.79 | kg |

**\(I\) 是什么**：质量矩阵右下角是 \(I+Ml^2\)。\(I=I_{\mathrm{cm}}\)（绕质心）；\(Ml^2\) 才是质心到轮轴。不是 \(\tfrac12 ML^2\)。质点模型 \(I=0\)，绕轴惯量只剩 \(Ml^2=2.91\times10^{-4}\)。  
分段（不把零件当有尺寸刚体，只计质心偏移）：

\[
I_{\mathrm{cm}}\approx\sum m_i(h_i-l)^2 \approx 5.63\times 10^{-4}\ \mathrm{kg\cdot m^2}
\]

**质量矩阵（与 `lqr_balance_theory.html` 4.5 同一套，\(I=0\)）**：

\[
\mathbf{M}_q=
\begin{bmatrix}
M+m & Ml \\
Ml & I+Ml^2
\end{bmatrix}
=
\begin{bmatrix}
0.790 & 0.0143 \\
0.0143 & 2.91\times 10^{-4}
\end{bmatrix}
,\quad
Mgl=0.140,\quad
1/r=26.7
\]

用分段 \(I_{\mathrm{cm}}\) 时只改右下角为 \(8.55\times10^{-4}\)。\(\tau\) 为两轮轮轴力矩之和（N·m）。

---

## 5. 称重顺序（建议）

```text
1) 整车（带轮）称一次 → 总重
2) 拆左右轮，分别称轮 → 填 §3
3) 去轮车体上秤 → 应等于 M（§2 合计）
4) 再拆电池 / 板 /（可选）骨架分称 → 填 §2 各行 + 量 h_i
5) 算 l_M，写入 §4
```

不必为测 \(M\) 把电机从车上拧下来；**只拆轮**即可。
